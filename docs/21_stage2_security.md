# 阶段 2 安全

上传、版本更新、审核、激活、停用和任务列表仅限管理员；限制扩展名和大小，使用 SHA-256 重命名存储文件，绝不返回服务器路径。HTML 不执行脚本；PDF/DOCX 只抽取文本。居民不可枚举文档，只能通过其绑定小区范围检索有效资料；历史检索仅客服/管理员可用。文档中的指令仅作为文本证据，绝不作为系统指令执行；可疑 Chunk 不进入居民检索。问题日志先脱敏，反馈只允许查询发起人提交，且按保留期限清理。
