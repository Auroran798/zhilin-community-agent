# 阶段 3 测试报告

2026-08-03 实际执行 `python -m pytest -q`：18 passed、0 failed、0 skipped。`tests/test_stage3_agent.py` 覆盖：报修预览、LangGraph 确认恢复、重复确认幂等、预览修改后再次确认、取消、记忆同意/删除、多轮字段合并、电梯困人追问、账单差额比较、公告双版本草稿、普通巡检记录与自动整改工单、高风险积水靠近配电柜转人工、工单取消/评价、RAG 引用元数据、人工处理单分派/结案、工具调用审计和会话越权隔离。阶段 1/2 测试继续由全量回归覆盖。

验收入口：`python scripts/check_agent.py`。该命令生成 95 条离线评测集、计算 16 项指标后，再执行完整回归。测试使用 Fake LLM；不依赖外部模型服务或网络。

